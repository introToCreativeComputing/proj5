import React from 'react';
import RippleSketch from '../../components/RippleSketch'; // Correct the path to go one level up

const MainPage = () => {
    return (
        <div>
            <RippleSketch />
        </div>
    );
};

export default MainPage;
